# ==========================================
# Library Management System (ЦБС Вологда)
# Environment Configuration
# ==========================================
# Копируйте этот файл в .env и настройте значения
# cp .env.example .env

# ==========================================
# DATABASE (Обязательно!)
# ==========================================
# Формат PostgreSQL: postgresql+asyncpg://user:password@host:port/dbname
# Для SQLite (только dev): sqlite+aiosqlite:///./library.db
DATABASE_URL=postgresql+asyncpg://library_user:strong_password@localhost:5432/library_db

# ==========================================
# JWT SECURITY (Обязательно!)
# ==========================================
# SECRET_KEY для подписи JWT токенов. ДОЛЖЕН быть не менее 32 символов!
# Генерация: openssl rand -hex 32
SECRET_KEY=change-me-to-64-char-hex-string-from-openssl-rand-hex-32

# Алгоритм подписи JWT (рекомендуется HS256)
ALGORITHM=HS256

# Время жизни access токена в минутах
ACCESS_TOKEN_EXPIRE_MINUTES=20

# Время жизни refresh токена в днях
REFRESH_TOKEN_EXPIRE_DAYS=7

# ==========================================
# APPLICATION
# ==========================================
# Режим отладки (True/False). В production ОБЯЗАТЕЛЬНО False!
DEBUG=False

# ==========================================
# BACKUP CONFIGURATION (PG-005)
# ==========================================
# Директория для хранения бэкапов PostgreSQL
BACKUP_DIR=./backups

# Сколько дней хранить бэкапы (автоматическое удаление старых)
BACKUP_RETENTION_DAYS=7

# ==========================================
# PRODUCTION CHECKLIST
# ==========================================
# Перед деплоем в production проверьте:
# [ ] DATABASE_URL указывает на production PostgreSQL
# [ ] SECRET_KEY сгенерирован через openssl rand -hex 32
# [ ] SECRET_KEY не равен дефолтному из этого файла
# [ ] DEBUG=False
# [ ] Порт и хост настроены корректно
# [ ] BACKUP_DIR настроен и доступен для записи
# [ ] Настроен cron для регулярных бэкапов (crontab -e):
