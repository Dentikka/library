# Library — Сводка статуса проекта

**Дата:** 2026-02-25  
**Статус:** ✅ MVP Готов к тестированию

---

## Общий прогресс

| Компонент | Статус | Ветка/Коммит |
|-----------|--------|--------------|
| Backend | ✅ Готово | main |
| Database | ✅ Готово | main |
| Auth (JWT) | ✅ Готово | main |
| Search API | ✅ Готово | main |
| Frontend | ✅ Готово | main |
| Tests | ⏳ Ожидается | - |

---

## Backend (100%)

### Модели БД
- ✅ Author — авторы книг
- ✅ Library — филиалы библиотек
- ✅ Book — книги с search_vector
- ✅ Copy — экземпляры (инв. номера)
- ✅ StaffUser — сотрудники с JWT

### API Endpoints

**Auth:**
- `POST /api/v1/auth/login` — вход
- `POST /api/v1/auth/refresh` — обновление токена
- `GET /api/v1/auth/me` — текущий пользователь
- `POST /api/v1/auth/register` — регистрация (admin)

**Authors:**
- `GET /api/v1/authors` — список авторов
- `POST /api/v1/authors` — создание (staff)

**Books:**
- `GET /api/v1/books` — список книг (с пагинацией skip/limit)
- `GET /api/v1/books/{id}` — детали книги
- `POST /api/v1/books` — создание (staff)
- `PUT /api/v1/books/{id}` — обновление (staff)
- `DELETE /api/v1/books/{id}` — удаление (staff)

**Copies:**
- `GET /api/v1/books/{id}/copies` — экземпляры книги
- `POST /api/v1/books/{id}/copies` — добавление (staff)
- `PUT /api/v1/books/copies/{id}` — обновление (staff)
- `DELETE /api/v1/books/copies/{id}` — удаление (staff)

**Libraries:**
- `GET /api/v1/libraries` — список библиотек
- `GET /api/v1/libraries/{id}` — детали
- `POST /api/v1/libraries` — создание (staff)
- `PUT /api/v1/libraries/{id}` — обновление (staff)
- `DELETE /api/v1/libraries/{id}` — удаление (staff)

**Search:**
- `GET /api/v1/search?q=` — поиск
- `GET /api/v1/search/suggestions` — автокомплит
- `GET /api/v1/search/advanced` — расширенный поиск

---

## Frontend (100%)

### Страницы

| Страница | URL | Описание |
|----------|-----|----------|
| Главная | `/` | Поиск, статистика, библиотеки |
| Поиск | `/search?q=` | Результаты поиска |
| Детали книги | `/books/{id}` | Информация + наличие |
| Библиотеки | `/libraries` | Список филиалов |
| Вход | `/staff/login` | JWT авторизация |
| Панель | `/staff/dashboard` | Управление книгами |

### Технологии
- Tailwind CSS (CDN)
- Lucide Icons
- Inter font
- HTMX
- Vanilla JS

---

## Тестовые данные

### Библиотеки (3)
1. Центральная библиотека им. В.И. Белова
2. Библиотека на Панкратова
3. Библиотека на Добролюбова

### Авторы (10)
Лев Толстой, Федор Достоевский, Александр Пушкин, Антон Чехов, Михаил Булгаков, Николай Гоголь, Иван Тургенев, Виктор Гюго, Джордж Оруэлл, Эрнест Хемингуэй

### Книги (20)
Классика русской и мировой литературы

### Экземпляры (41)
1-3 копии на книгу в разных библиотеках

### Пользователи
- `admin` / `admin123` — администратор
- `librarian1` / `lib123` — библиотекарь
- `librarian2` / `lib123` — библиотекарь

---

## Запуск проекта

```bash
# Установка зависимостей
pip install -r requirements.txt

# Применение миграций
alembic upgrade head

# Заполнение тестовыми данными
python scripts/seed.py

# Запуск сервера
uvicorn app.main:app --reload

# Открыть в браузере
open http://localhost:8000
```

---

## Git репозиторий

**URL:** https://github.com/Dentikka/library

**Структура:**
```
library/
├── app/                   # FastAPI приложение
│   ├── models.py         # SQLAlchemy модели
│   ├── database.py       # Подключение к БД
│   ├── config.py         # Настройки
│   ├── main.py           # Точка входа
│   ├── routers/          # API endpoints
│   ├── schemas/          # Pydantic схемы
│   └── services/         # Бизнес-логика
├── templates/            # Jinja2 шаблоны
│   ├── base.html
│   ├── index.html
│   ├── search.html
│   ├── book_detail.html
│   ├── libraries.html
│   └── staff/
│       ├── login.html
│       └── dashboard.html
├── alembic/              # Миграции БД
├── scripts/              # Скрипты
│   └── seed.py
├── requirements.txt
├── pyproject.toml
├── alembic.ini
├── docker-compose.yml
└── README.md
```

---

## Что дальше?

### Phase 2 — Enhancement ✅ ЗАВЕРШЁН (25.02.2026)

**🔴 ВЫСОКИЙ приоритет:**
- [x] Пагинация результатов поиска (20 книг/страницу) — ✅ Протестировано
- [x] CRUD книг в панели сотрудника — ✅ Протестировано

**🟡 СРЕДНИЙ приоритет:**
- [x] Загрузка обложек книг — ✅ Протестировано (2 некритичных бага)
- [x] Мобильная адаптация (responsive) — ✅ Протестировано

**QA Отчёт:** `docs/QA_REPORT_PHASE2.md`

**✅ Исправленные баги:**
- ~~BUG-1: Пустой title принимается~~ — ✅ Исправлено (коммит a6eb019)
- ~~BUG-2: Валидация content-type обложки не работает~~ — ✅ Исправлено (коммит a6eb019)

**❌ Исключено:**
- История поиска, избранное, Docker

---

### Phase 3 — Production-ready 🚨 ЗАБЛОКИРОВАНО (25.02.2026 20:50)

**Отчёт тимлида:** 2026-02-25 20:50

**🔴 ВЫСОКИЙ приоритет:**
- [x] **PG-001:** PostgreSQL миграция — ✅ **ГОТОВО**
- [x] **PG-001-FIX:** Fix alembic.ini — ✅ **ГОТОВО**
- [x] **PG-002:** Environment configuration — ✅ **ГОТОВО**
- [x] **PG-002-FIX:** Security fixes — ✅ **ГОТОВО**
- [ ] **PG-003:** HTTPS + SSL — ⏳ **ЗАБЛОКИРОВАНО** (требуется сервер)

**⚠️ Блокеры:**
- 🔴 **BLK-002:** Нет сервера для HTTPS — требуется решение пользователя

**🟡 СРЕДНИЙ приоритет (альтернатива при блокировке):**
- [ ] **PG-004:** Monitoring (логи, метрики)
- [ ] **PG-005:** Backup strategy
- [ ] **PG-006:** Performance optimization

**🔴 ВЫСОКИЙ приоритет (альтернатива при блокировке PG-003):**
- [x] **BUG-001:** Fix BUG-1 (валидация title) — ✅ Исправлено
- [x] **BUG-002:** Fix BUG-2 (валидация content-type) — ✅ Исправлено

**🟡 СРЕДНИЙ приоритет (альтернатива при блокировке):**
- [ ] **PG-004:** Monitoring (логи, health check)
- [ ] **PG-005:** Backup strategy

---

## Команда

| Роль | Исполнитель | Текущая задача |
|------|-------------|----------------|
| Тимлид | MoltBot | Координация альтернативного спринта |
| Backend | MoltBot | **BUG-1:** Валидация title |
| Frontend | MoltBot | 🟡 В паузе |
| QA | MoltBot | Готов к тестированию баг-фиксов |

---

**Статус:** Phase 2 ✅ завершён. Phase 3 🚀 запущен (высокий приоритет).
